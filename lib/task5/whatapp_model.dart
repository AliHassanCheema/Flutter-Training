class WhatsappModel {
  String image;
  String contactName;
  String lastMsg;
  String lastSeen;
  String groupName;
  String lastCall;
  String lastGroupMsg;
  WhatsappModel(this.image, this.contactName, this.lastMsg, this.lastSeen,
      this.groupName, this.lastCall, this.lastGroupMsg);
}
